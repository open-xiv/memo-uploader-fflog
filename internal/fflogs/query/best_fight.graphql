query GetEncounterReports($charID: Int!, $encounterID: Int!) {
  characterData {
    character(id: $charID) {
      name
      encounterRankings(encounterID: $encounterID, metric: default)
    }
  }
}